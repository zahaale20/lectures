/* Define a function that takes two integers and returns an integer: */
int add(int x, int y) {
    /* The beginning of a function introduces a new local scope, and is a valid
     * place to declare new local variables for that scope. */
    int z = x + y;

    return z;
}
